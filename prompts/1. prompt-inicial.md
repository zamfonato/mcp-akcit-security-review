<role>
Você é um engenheiro de software sênior especializado em TypeScript moderno,
Node.js e no Model Context Protocol (MCP) da Anthropic. Neste prompt você atua
como **arquiteto de scaffolding**, não como implementador de funcionalidades.
Pense como quem monta a fundação.
</role>

<context>
Estou construindo um servidor MCP em TypeScript como trabalho prático do
Módulo 4 do curso de Especialização em Engenharia de Software com IA
Generativa (AKCIT / UFG, 2026).

**Objetivo de negócio do projeto (não desta iteração):** auditar dependências
Node de uma codebase, consultar bases públicas por vulnerabilidades
conhecidas, e gerar um relatório `SECURITY-REVIEW.md` com análise de
*exploitability contextual* — o LLM cliente avalia não só a severidade do CVE,
mas como o pacote vulnerável é de fato usado no código.

**Arquitetura:** o servidor **NÃO chama LLMs diretamente**. Expõe:
  - MCP Tools para coleta de dados (listar deps, buscar CVEs, grepar uso)
  - MCP Prompts (slash commands) que são templates de kickoff;
    a orquestração agêntica fica no cliente (Claude Code).

**Repositório:** https://github.com/zamfonato/mcp-akcit-security-review
(atualmente vazio, recém-clonado).

**Stack decidida:**
  - Node 22 LTS
  - TypeScript + ESM puro
  - npm como package manager
  - vitest para testes
  - Nome do pacote: `@zamfonato/mcp-akcit-security-review`
  - Licença: MIT, titular: Pedro Santos, ano 2026
  - Conventional Commits

**Referências de qualidade/segurança a respeitar**:
  - OWASP Top 10 for LLM Applications 2025
  - NIST AI 600-1 (Generative AI Profile) — validação contínua + revisão humana
</context>

<task>
Escopo **apenas desta iteração** — não vá além do listado:

1. Criar a estrutura de pastas mínima do projeto.
2. Gerar `package.json` com as dependências **exatas** para um servidor MCP
   stdio em TypeScript (sem libs "por via das dúvidas").
3. Gerar `tsconfig.json` adequado a ESM + Node 22 + modo estrito.
4. Criar `src/index.ts` com um servidor MCP **funcional porém vazio**:
   responde a `ListTools` com `[]`, a `ListPrompts` com `[]`, e inicializa
   via transporte stdio. Nada mais.
5. Gerar `.gitignore` multi-stack (Node + VS Code + arquivos de SO).
6. Gerar `LICENSE` MIT (titular: Pedro Santos, ano 2026).
7. Gerar um `README.md` **stub** — apenas título, parágrafo de descrição
   e seção "Status: em construção". README completo fica para o fim do
   projeto

**Explicitamente NÃO faça** nesta iteração:
  - Não implementar tools reais.
  - Não implementar prompts MCP reais.
  - Não chamar apis reais
  - Não sugerir Dockerfile, GitHub Actions, nem outro delivery.
  - Não adicionar libs "defensivas" (dotenv, winston, axios, node-fetch).
  - Não gerar testes ainda (vitest entra como devDep, mas zero `.test.ts`).
</task>

<reasoning_strategy>
Aplique **Skeleton-of-Thought**:

ETAPA 1 — Gere **apenas** o esqueleto: lista hierárquica dos arquivos que
          você vai criar, cada um com 1-2 linhas explicando propósito
          e a ordem em que devem ser gerados.
ETAPA 2 — **PARE e aguarde minha aprovação explícita.** Não expanda para
          conteúdo de arquivos sem minha aprovação.

Se durante a ETAPA 1 houver **qualquer decisão ambígua** (versão atual do
SDK `@modelcontextprotocol/sdk`, formato preferido de config, etc.), **liste
as ambiguidades como perguntas em um bloco `<open_questions>`** em vez de
adivinhar. Prefira perguntar do que alucinar.
</reasoning_strategy>

<constraints>
**Restrições técnicas:**
  - Apenas pacotes npm que você tem certeza absoluta que existem na versão
    indicada. Se não souber a versão atual de `@modelcontextprotocol/sdk`,
    pesquie na interenet.
  - Zero dependências decorativas. Só o estritamente necessário para um
    servidor MCP vazio iniciar via stdio.
  - `tsconfig.json` com `"strict": true`, `"noUncheckedIndexedAccess": true`
    e configuração ESM moderna compatível com Node 22.
  - Nome do pacote: `@zamfonato/mcp-akcit-security-review`. Privado? Não —
    será publicado público no GitHub; `"private": false` no package.json.

**Restrições de forma da resposta:**
  - Cada arquivo em seu próprio bloco, precedido do path relativo em `####`.
  - Sem comentários decorativos no código. Comentário só quando o *porquê*
    não for óbvio (ex: quirk conhecido do MCP SDK).
  - Sem código `TODO`/`FIXME` — ou faz agora ou não faz.
  - Sem emojis em qualquer arquivo gerado. Principalmente readme

**Restrições de escopo:**
  - O que não está no `<task>` não deve aparecer no output.
  - Se identificar algo que seria bom ter mas está fora de escopo, liste
    em `<out_of_scope_suggestions>` no final — não implemente.
</constraints>

<anti_hallucination_checklist>
Checklist que você deve aplicar **internamente** antes de responder:

  [ ] Toda dep npm que listei existe de fato e a versão é plausível
      para abril/2026.
  [ ] API do `@modelcontextprotocol/sdk` que usei no `src/index.ts`
      corresponde à spec MCP real — se incerto, explicitei a dúvida
      em `<open_questions>`.
  [ ] Não inventei feature do SDK que talvez não exista.
  [ ] `tsconfig.json` gerado compila com Node 22 + ESM sem warning.
  [ ] Não inventei arquivo de configuração fictício
      (ex: um `mcp.config.json` que o SDK não usa).
  [ ] "Não sei" é uma resposta aceitável e está explícito onde aplicável.
  [ ] Apenas informações fornecidas nesta conversa + conhecimento
      verificável sobre TypeScript/Node/MCP SDK foram usados.

Se algum item do checklist falhar, PARE e pergunte em vez de prosseguir.
</anti_hallucination_checklist>

<output_format>
Responda **nesta ordem**, nesta estrutura:

1. `<skeleton>` — árvore de arquivos propostos com 1-2 linhas por arquivo.
2. `<open_questions>` — perguntas antes de prosseguir (vazio se não houver).
3. `<out_of_scope_suggestions>` — ideias fora de escopo desta iteração
   (opcional).
4. **PARE.** Aguarde minha aprovação antes de gerar o conteúdo dos arquivos.

Na próxima rodada (após minha aprovação), cada arquivo vem em:

#### caminho/relativo/do/arquivo.ext
```linguagem
conteúdo do arquivo
```
</output_format>

<acceptance_criteria>
Critérios objetivos que usarei para avaliar a saída:

  - **Coerência (lógica interna):** `tsconfig` bate com `package.json`;
    `src/index.ts` compila sob o `tsconfig` proposto; scripts npm
    funcionam.
  - **Pertinência (adere ao pedido):** nada fora do escopo `<task>`;
    nada a mais, nada a menos.
  - **Alinhamento (espírito do pedido):** respeita stack declarada,
    convenções (ESM, strict, MIT, scoped), e a filosofia "fundação
    mínima que roda".
  - **Verificação operacional:** se eu rodar na sequência
    `npm install && npm run build && node dist/index.js`, o servidor
    deve iniciar e ficar aguardando mensagens MCP em stdin sem crashar.
</acceptance_criteria>