<role>
Você é um engenheiro TypeScript fechando a funcionalidade de um servidor
MCP. Papel de implementador. Os elementos arquiteturais já existem
(scaffold, primeira tool, fixtures reais da OSV, separação fetch/normalize)
e devem ser respeitados. Esta iteração é a maior do projeto.
</role>

<context>
Continuação dos prompts 1 e 2. Estado atual do repositório:
  - Servidor MCP scaffolded (stdio, ESM, Node 22, TS estrito)
  - Tool `fetch_vulnerabilities` implementada com schema Zod, validação
    Zod da resposta crua da OSV.dev, normalização pura separada
    (`src/lib/osv-normalize.ts`), 7 testes verdes
  - Fixtures reais em `tests/fixtures/osv/`

**Objetivo desta iteração:** completar funcionalmente a ferramenta. Após o
prompt, um usuário do Claude Code deve poder invocar
`/doc-security-review` em um projeto Node, e receber um arquivo
`SECURITY-REVIEW.md` no projeto auditado, com:
  - Auditoria de TODAS as deps (diretas, dev, peer, transitivas via lockfile)
  - Cada CVE classificado em categoria OWASP Top 10 (Web)
  - Lente OWASP LLM Top 10 (2025) ativada se o projeto usa AI APIs
  - Sugestão de upgrade contextual (versão mínima segura vs última estável,
    com sinal de major jump)
  - Pacotes deprecated sinalizados explicitamente

**Endpoint adicional descoberto e validado** (ver "Dados empíricos" acima
desta seção):
  - `GET https://registry.npmjs.org/<pkg>/latest` → manifest da última
    versão. Inclui campo `deprecated: string` quando aplicável.
  - HTTP 404 retorna texto "Not Found" (não JSON) — pacote não existe no
    registry.

**Stack e convenções estabelecidas** (não reabrir):
  - TypeScript estrito + ESM puro + Node 22
  - Zod para validação de schemas
  - vitest para testes
  - `@modelcontextprotocol/sdk` v1.29.0
  - `fetch` global (NÃO adicionar `node-fetch`/`axios`)
  - Mensagens de erro de domínio em português
  - JSON Schema escrito à mão para inputSchema (sem `zod-to-json-schema`)
  - Tools retornam `{content:[{type:"text",text:JSON.stringify(...)}],
    structuredContent: result}` em sucesso, e `{content:[...], isError:true}`
    em erro de domínio
</context>

<task>
Implementar **tudo o que segue, exatamente nesta divisão**, sem expandir
escopo:

### Bloco A — Tool `fetch_latest_version`

A1. Criar `src/lib/npm-registry-normalize.ts`:
    - Schema Zod parcial da resposta de `/<pkg>/latest` (apenas campos
      consumidos: `name`, `version`, `deprecated`, `license`, `homepage`,
      `repository.url`).
    - Função pura `normalizeNpmManifest(raw, pkg)` retornando objeto
      conforme `<examples>` (output do tool).
    - SEM rede, SEM `Date.now`. Quem chama injeta `queried_at`.

A2. Criar `src/tools/fetch-latest-version.ts`:
    - Schema Zod input: `{ name: string, ecosystem: "npm" }`. (NÃO recebe
      version — queremos saber a última.)
    - JSON Schema escrito à mão (3 campos, trivial).
    - Função `fetchLatestVersion(input)`:
      a) GET para `https://registry.npmjs.org/<encodeURIComponent(name)>/latest`
      b) Trata HTTP 200 (parse JSON, valida Zod, normaliza), HTTP 404
         (pacote inexistente — erro de domínio "Pacote X não encontrado
         no registry npm"), outros 4xx/5xx (erro de domínio com status),
         erro de rede.
      c) Timeout 10s via `AbortController`.
    - Constantes `FETCH_LATEST_VERSION_TOOL` e `TOOL_INPUT_JSON_SCHEMA`.

A3. Atualizar `src/index.ts`:
    - Adicionar a tool nova ao array de `ListToolsRequestSchema`.
    - Atualizar o switch de `CallToolRequestSchema` para rotear ambas as
      tools.
    - **NÃO** quebrar a tool já existente `fetch_vulnerabilities`.

### Bloco B — Data files estáticos OWASP

B1. Criar `src/data/owasp-web.ts`:
    - Exportar `OWASP_WEB_TOP_10` como array typed de 10 entradas com:
      `id` ("A01".."A10"), `title` (texto oficial em inglês),
      `short_description` (1-2 frases, parafraseado, NÃO copy-paste literal),
      `top_cwe_ids` (lista de **até 10** CWE IDs mais relevantes pra cada
      categoria conforme mapping oficial OWASP — fonte:
      https://owasp.org/Top10/).
    - Exportar tipo `OwaspWebCategory`.
    - Exportar função `findWebCategoryByCwe(cwe: string): OwaspWebCategory | null`
      que busca em `top_cwe_ids` de cada categoria.
    - Versão: usar **OWASP Top 10 2021** (versão estável publicada).

B2. Criar `src/data/owasp-llm.ts`:
    - Exportar `OWASP_LLM_TOP_10_2025` como array typed de 10 entradas:
      `id` ("LLM01".."LLM10"), `title`, `short_description`.
    - Sem CWE mapping (LLM Top 10 não tem mapping CWE estabelecido —
      assumimos correlação semântica feita pelo LLM cliente).
    - Versão: **OWASP LLM Top 10 2025** (publicada).

B3. Criar `src/data/ai-stack-deps.ts`:
    - Exportar `AI_STACK_DEPS` como `readonly string[]` com nomes de
      pacotes npm que indicam uso de AI/LLM. Cobrir os mais comuns:
      `openai`, `@openai/agents`, `@anthropic-ai/sdk`, `anthropic`,
      `langchain`, `@langchain/core`, `@langchain/community`,
      `llamaindex`, `ai` (Vercel AI SDK), `groq-sdk`, `cohere-ai`,
      `@google/generative-ai`, `@aws-sdk/client-bedrock-runtime`,
      `@mistralai/mistralai`, `replicate`, `together-ai`.
    - Função `hasAiStack(depNames: readonly string[]): boolean` —
      retorna true se interseção não-vazia.
    - **NÃO inventar nomes de pacote.** Se não souber se o nome correto
      é `@anthropic-ai/sdk` ou `@anthropic/sdk`, PARE e pergunte.

### Bloco C — Slash command `/doc-security-review`

C1. Criar `src/prompts/doc-security-review.ts`:
    - Constante `DOC_SECURITY_REVIEW_PROMPT = "doc-security-review"`.
    - Função `renderDocSecurityReviewPrompt(args)` que retorna o objeto
      MCP Prompt no formato exigido pela spec:
      `{ description, messages: [{ role: "user", content: { type: "text", text: <template renderizado> } }] }`.
    - Argumento opcional aceito: `repo_path` (string). Se não informado,
      o template instrui o cliente a usar o diretório atual.
    - O template deve **embutir como texto**:
      a) Persona ("Você é um auditor de segurança...").
      b) Instruções operacionais passo-a-passo: ler `package.json`,
         identificar deps em `dependencies` + `devDependencies` +
         `peerDependencies`; ler `package-lock.json` se existir e extrair
         versões resolvidas (incluindo transitivas); deduplicar
         pacote+versão; chamar `fetch_vulnerabilities` para cada par
         único; chamar `fetch_latest_version` para cada nome único;
         comparar versões com semver (sinalizar major jump);
         classificar cada CVE/CWE em categoria OWASP Web; se a lista
         de deps interceptar `AI_STACK_DEPS`, ativar lente OWASP LLM.
      c) Conteúdo completo de `OWASP_WEB_TOP_10` formatado como
         tabela markdown dentro do template.
      d) Conteúdo completo de `OWASP_LLM_TOP_10_2025` igualmente.
      e) Lista `AI_STACK_DEPS` como referência inline.
      f) Especificação do formato esperado do `SECURITY-REVIEW.md`
         conforme `<examples>`.
      g) Diretrizes anti-alucinação: se um CWE não casar com nenhuma
         categoria via `top_cwe_ids`, o LLM cliente pode inferir, mas
         deve marcar como "(inferred)"; se uma vulnerabilidade não tem
         CWE conhecido, agrupar em "Outros / Sem categorização OWASP".

C2. Atualizar `src/index.ts`:
    - Adicionar `GetPromptRequestSchema` ao import.
    - Atualizar handler de `ListPromptsRequestSchema` para incluir o
      slash command com `arguments` listando `repo_path` (optional).
    - Adicionar handler de `GetPromptRequestSchema` que:
      a) Verifica se o nome é `doc-security-review`.
      b) Chama `renderDocSecurityReviewPrompt(request.params.arguments)`.
      c) Retorna o objeto.
      d) Caso contrário, lança erro de "prompt desconhecido".

### Bloco D — Testes unitários (foco principal desta iteração)

D1. `tests/lib/npm-registry-normalize.test.ts`:
    - Mínimo 3 casos: manifest completo → normalizado correto;
      manifest sem `repository` → `repository_url: null`;
      manifest com `deprecated` string → `is_deprecated: true` e
      `deprecation_message` preservado.

D2. `tests/tools/fetch-latest-version.test.ts`:
    - Mocka `global.fetch` via `vi.stubGlobal`. Fake timers para
      `queried_at` determinístico.
    - Mínimo 4 casos:
      (a) happy path lodash — fetch resolve com fixture, saída bate
          com schema esperado.
      (b) pacote deprecado — fetch resolve com manifest contendo
          `deprecated`, output tem `is_deprecated: true`.
      (c) HTTP 404 — fetch resolve com status 404 e body texto
          "Not Found", tool retorna erro de domínio em PT-BR
          mencionando "não encontrado no registry npm".
      (d) erro de rede — fetch rejeita, tool retorna erro de domínio.

D3. `tests/data/owasp-web.test.ts`:
    - Mínimo 3 casos: array tem exatamente 10 entradas com IDs
      "A01".."A10" únicos; toda entrada tem `top_cwe_ids` não-vazio;
      `findWebCategoryByCwe("CWE-89")` retorna a categoria de
      Injection (A03).

D4. `tests/data/owasp-llm.test.ts`:
    - Mínimo 2 casos: array tem 10 entradas IDs "LLM01".."LLM10";
      cada entrada tem `title` e `short_description` não-vazios.

D5. `tests/data/ai-stack-deps.test.ts`:
    - Mínimo 3 casos: lista não-vazia; `hasAiStack(["express"])` é
      false; `hasAiStack(["express","openai"])` é true.

D6. `tests/prompts/doc-security-review.test.ts`:
    - Mínimo 4 casos:
      (a) Template renderizado contém o título de cada categoria
          OWASP Web (assert por contém-strings — 10 asserts).
      (b) Template renderizado contém o título de cada categoria
          OWASP LLM (10 asserts).
      (c) Template renderizado menciona explicitamente os arquivos
          `package.json`, `package-lock.json`, e `peerDependencies`.
      (d) Quando `repo_path` é passado, template inclui esse path
          literal; quando não é, template instrui usar o diretório
          atual.

### Fixtures novos
- `tests/fixtures/npm/lodash-latest.json` — capturei a resposta real
  de `https://registry.npmjs.org/lodash/latest` em 2026-04-25.
  **Eu vou colar abaixo na ETAPA 2 quando você pedir** ou você pode
  capturar via curl você mesmo se tiver acesso a rede; salve EXATAMENTE
  como está.
- `tests/fixtures/npm/deprecated-package.json` — resposta real de
  um pacote deprecado (ex: `request/latest`). **Mesmo procedimento.**

### Explicitamente NÃO faça
  - NÃO mexer em `README.md`, `LICENSE`, `.gitignore`.
  - NÃO criar release tag, GitHub Actions, Dockerfile.
  - NÃO implementar cache de respostas (das duas tools).
  - NÃO usar endpoint batch (`/v1/querybatch` da OSV).
  - NÃO adicionar suporte a Cargo, PyPI, Maven.
  - NÃO adicionar deps externas. Tudo com o que já existe.
  - NÃO escrever testes de integração que batem em rede real.
  - NÃO usar `any`, NÃO usar `as` sem comentário justificando.
  - NÃO escrever comentários decorativos.
  - NÃO copiar texto literal extenso da documentação OWASP — paráfrase
    em 1-2 frases por categoria evita problema de copyright e mantém
    o template enxuto.
</task>

<reasoning_strategy>
Aplique **Skeleton-of-Thought** combinado com **Context-Aware
Decomposition**. A tarefa tem 4 blocos (A, B, C, D) e cada bloco tem
sub-itens. NÃO mistura — resolve um bloco por vez, em ordem.

**ETAPA 1** — entregue:
  1. `<plan>` com lista de TODOS os arquivos a criar/modificar
     agrupados por bloco (A/B/C/D) e ordem de criação.
  2. `<open_questions>` com qualquer dúvida que possa fazer você
     adivinhar — em especial:
     - Versões oficiais OWASP (Top 10 Web — 2021 confirmado;
       LLM Top 10 — 2025 confirmado)
     - Mapping CWE específico onde houver ambiguidade
     - Nomes exatos de pacotes em `AI_STACK_DEPS` se incerto
     - Forma exata da resposta `GetPromptRequestSchema` no SDK
       MCP v1.29.0
  3. PARE. Aguarde minha aprovação antes da ETAPA 2.

**ETAPA 2** (após aprovação) — produza arquivos em ordem de bloco
(A → B → C → D), respeitando dependências entre eles (B precisa estar
antes de C porque C importa B; A pode vir antes ou depois de B/C
indistintamente).
</reasoning_strategy>

<examples>
Few-shot do output da nova tool e do formato esperado do
`SECURITY-REVIEW.md`.

**Output esperado de `fetch_latest_version`** (input `{name:"lodash",
ecosystem:"npm"}`):

```json
{
  "package": { "name": "lodash", "ecosystem": "npm" },
  "latest_version": "4.18.1",
  "is_deprecated": false,
  "deprecation_message": null,
  "license": "MIT",
  "homepage": "https://lodash.com/",
  "repository_url": "https://github.com/lodash/lodash.git",
  "queried_at": "2026-04-25T17:00:00.000Z"
}
```

**Caso deprecated** (input `{name:"request", ecosystem:"npm"}`):

```json
{
  "package": { "name": "request", "ecosystem": "npm" },
  "latest_version": "2.88.2",
  "is_deprecated": true,
  "deprecation_message": "request has been deprecated, see https://github.com/request/request/issues/3142",
  "license": "Apache-2.0",
  "homepage": "https://github.com/request/request#readme",
  "repository_url": "https://github.com/request/request.git",
  "queried_at": "2026-04-25T17:00:00.000Z"
}
```

**Caso 404** — retorna MCP tool result com `isError:true` e mensagem
`"Pacote 'xxx' não encontrado no registry npm (HTTP 404)."`.

---

**Estrutura mínima esperada do `SECURITY-REVIEW.md` gerado pelo
slash command**:

```markdown
# Security Review — <nome do projeto>

**Gerado em:** <ISO 8601>
**Auditor:** mcp-akcit-security-review v0.1.0
**Escopo:** dependencies + devDependencies + peerDependencies (diretas
e transitivas via package-lock.json)

## Resumo Executivo
- <N> pacotes auditados (<N_diretos> diretos, <N_trans> transitivos)
- <N> vulnerabilidades encontradas (<N_critical> CRITICAL,
  <N_high> HIGH, <N_moderate> MODERATE, <N_low> LOW)
- <N> categorias OWASP Web Top 10 afetadas
- Lente OWASP LLM Top 10: <ATIVADA por presença de openai/anthropic-ai|
  NÃO ATIVADA — projeto não usa AI APIs detectadas>
- <N> pacotes deprecated em uso

## OWASP Web Top 10 — Findings agrupados

### A06 — Vulnerable and Outdated Components (<N> findings)
#### lodash @ 4.17.11 — <N> CVEs
- **GHSA-29mw-wpgm-hmr9** (CVE-2020-28500, MODERATE)
  - ReDoS via toNumber/trim/trimEnd — CWE-1333
  - Versão mínima segura (OSV): 4.17.21
  - Última disponível (npm): 4.18.1
  - Recomendação: upgrade pra 4.18.1 (mesmo major, baixo risco)

### A03 — Injection (<N> findings)
...

## OWASP LLM Top 10 (2025) — apenas se ativada
### LLM03 — Supply Chain
...

## Pacotes Deprecated
- request@2.88.2 — "request has been deprecated, ..."
  - Migrar para axios, undici, ou fetch nativo

## Outros / Sem categorização OWASP
...

## Apêndice — Limitações
- Auditoria baseada exclusivamente em CVEs públicos da OSV.dev
- Não substitui Snyk/Dependabot/SAST
- LLM Top 10 mapeado por similaridade semântica (não há CWE oficial)
```

A IA cliente preenche todas as contagens e listas dinamicamente a
partir das chamadas de tool e dos data files OWASP embutidos no
template.
</examples>

<constraints>
  - Toda nova função pública/exportada com tipos TS explícitos. Zero
    inferência de retorno em nível de API pública.
  - Todas as chamadas de rede com `AbortController` 10s.
  - `getPrompt` handler não pode falhar silenciosamente — argumentos
    inválidos viram erro lançado com mensagem clara.
  - Data files OWASP devem ser **TypeScript com tipos exportados**, não
    JSON. Permite refactoring seguro.
  - Tudo "as const" ou `readonly` onde fizer sentido para imutabilidade.
  - Os data files NÃO podem ser strings monolíticas — cada categoria é
    objeto, e a renderização do template é responsabilidade do
    `doc-security-review.ts`.
  - Mensagens de erro de domínio em português, sempre começando com
    "Falha ao consultar registry npm:" ou "Pacote ... não encontrado..."
    para uniformidade com o estilo da tool 1.
  - O template do slash command em PT-BR (instruções pra Claude Code)
    e o `SECURITY-REVIEW.md` resultante também em PT-BR, mas IDs e
    títulos OWASP em inglês (são padrão internacional).
</constraints>

<anti_hallucination_checklist>
Antes de gerar conteúdo no Bloco B (OWASP), confirme:

  [ ] As 10 categorias OWASP Web Top 10 que vou listar são exatamente
      as do documento oficial 2021. Se eu lembrar errado, listei na
      <open_questions> em vez de chutar.
  [ ] As 10 categorias LLM Top 10 que vou listar são exatamente as do
      documento oficial 2025. Mesma regra.
  [ ] Os CWE IDs em cada `top_cwe_ids` saem do mapping oficial OWASP.
      Se eu não tenho certeza absoluta de algum, ele NÃO entra (melhor
      lista pequena correta que lista grande errada).
  [ ] Os nomes de pacote em `AI_STACK_DEPS` são os nomes reais publicados
      no npm (ex: `@anthropic-ai/sdk` está correto; eu confirmo ou
      pergunto).

Antes de gerar a tool `fetch_latest_version` (Bloco A), confirme:
  [ ] A URL do registry está correta:
      `https://registry.npmjs.org/<encodeURIComponent(name)>/latest`.
  [ ] O campo `deprecated` no manifest é uma string (não booleano)
      quando presente, ausente quando não deprecated.
  [ ] Não inventei campos no schema Zod que o registry npm não retorna.

Antes de implementar o slash command (Bloco C), confirme:
  [ ] O formato de retorno do handler `GetPromptRequestSchema` no SDK
      MCP v1.29.0 é exatamente:
      `{ description, messages: [{ role, content: { type: "text", text } }] }`.
      Se incerto, perguntei.
  [ ] O esquema de `arguments` do prompt na resposta de `ListPrompts`
      bate com a spec MCP (cada argumento tem `name`, `description`,
      `required`).

Geral:
  [ ] Nenhum arquivo fora dos blocos A/B/C/D foi tocado.
  [ ] `npm test` continuará passando para os 7 testes já existentes
      (não quebrei a tool 1).
</anti_hallucination_checklist>

<output_format>
**ETAPA 1** — entregue, nesta ordem:
  1. `<plan>` agrupado por bloco (A/B/C/D), listando arquivos.
  2. `<open_questions>` com dúvidas reais.
  3. PARE.

**ETAPA 2** (após aprovação), gere todos os arquivos em ordem de bloco
(A → B → C → D), e DENTRO de cada bloco em ordem que respeite
dependências de import. Cada arquivo em bloco próprio precedido de
`#### caminho/relativo/do/arquivo.ext`.

Ao final da ETAPA 2, inclua uma **mini-checklist de verificação** com:
  - Lista de arquivos novos/modificados.
  - Comando que devo rodar pra validar (`npm test` etc).
  - Itens do `<acceptance_criteria>` marcados.
</output_format>

<acceptance_criteria>
Três pilares — coerência, pertinência, alinhamento:

  - **Coerência:** todos os arquivos novos compilam juntos; tipos TS
    exportados nos data files são consumidos sem cast no
    `doc-security-review.ts`; tools registradas no `index.ts` batem
    com nomes usados nos testes.
  - **Pertinência:** todos os 4 blocos completos; nada de README, tag,
    cache, batch, outros ecossistemas.
  - **Alinhamento:** o servidor permanece honesto (não duplica
    capacidades do cliente); slash command é template + dados, não
    re-implementação de orquestração que o Claude Code faz nativamente.

**Verificação operacional que vou rodar:**
  - `npx tsc --noEmit` passa limpo.
  - `npm test` passa todos os testes (os 7 antigos + os novos do
    Bloco D), em verde, em <5s somados.
  - Registrando o servidor no Claude Code, `/doc-security-review`
    aparece como slash command. Invocar em um repo de teste produz
    um `SECURITY-REVIEW.md` com a estrutura do `<examples>`.
  - `fetch_latest_version` aparece em ListTools junto com
    `fetch_vulnerabilities`.
</acceptance_criteria>

<open_questions>
1. Fixtures npm — JÁ FORAM CAPTURADOS e estão no repo:
   - `tests/fixtures/npm/lodash-latest.json` (manifest real, 1669 bytes)
   - `tests/fixtures/npm/deprecated-package.json` (manifest real do
     `request/latest`, 3712 bytes, com campo `deprecated` populado:
     "request has been deprecated, see https://github.com/request/request/issues/3142")
   Não invente conteúdo; leia os arquivos como estão. Esta ETAPA 2 NÃO
   precisa criá-los nem o diretório `tests/fixtures/npm/`. Bloqueio
   liberado.

2. CWE mapping em `OWASP_WEB_TOP_10` — opção (a): conservador. Liste
   apenas IDs com certeza absoluta do mapping oficial 2021. Os exemplos
   que você sugeriu (CWE-79/89/78/77/94/917 em A03; CWE-1104/937 em A06;
   CWE-918 em A10) estão corretos — siga esse padrão. Para A04, A05 e
   A09, os 3-5 IDs core que você consegue defender chega — não force
   8-10 só pra encher.

3. OWASP LLM Top 10 2025 — sequência confirmada exatamente como
   listou:
     LLM01 Prompt Injection
     LLM02 Sensitive Information Disclosure
     LLM03 Supply Chain
     LLM04 Data and Model Poisoning
     LLM05 Improper Output Handling
     LLM06 Excessive Agency
     LLM07 System Prompt Leakage
     LLM08 Vector and Embedding Weaknesses
     LLM09 Misinformation
     LLM10 Unbounded Consumption
   Pode prosseguir.

4. `AI_STACK_DEPS` — dois ajustes confirmados:
   - REMOVA `anthropic` (sem escopo). Não existe esse pacote no npm
     como SDK oficial; era confusão com o nome do PyPI. Mantenha só
     `@anthropic-ai/sdk`.
   - INCLUA `@google/genai` (sucessor unificado, lançado em 2025) ao
     lado de `@google/generative-ai` (legado, ainda muito em uso).
     Cobre projetos antigos e novos.
   - Os outros 14 nomes da lista do prompt podem manter como estão.

5. Formato de retorno do `GetPromptRequestSchema` handler — confirmado.
   Retorne o objeto direto:
     `{ description, messages: [{ role: "user", content: { type: "text", text } }] }`
   O SDK serializa via `GetPromptResultSchema` internamente. Sem
   envelope `{ result: ... }`.

6. Erro pra prompt name desconhecido — confirmado: `throw new Error(...)`.
   O SDK converte em JSON-RPC error response. Mesmo princípio dos
   handlers de tool quando o input é inválido. Não retorne sucesso
   simulado com texto de aviso — confunde o cliente.

7. `structuredContent` em `fetch_latest_version` — SIM, mesmo padrão
   da tool 1. Sucesso retorna ambos:
     `content: [{ type: "text", text: JSON.stringify(result, null, 2) }]`
     `structuredContent: result`
   Erro de domínio retorna `{ content: [{ type: "text", text: msg }],
   isError: true }`.

PODE PROSSEGUIR PARA ETAPA 2.
</open_questions>